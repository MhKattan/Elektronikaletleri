@model IEnumerable<EntityLayer.Entities.Company>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h1>Firmalar Listesi</h1>

<!-- /.card-header -->
<div class="card-body p-0">
    <a asp-action="Create" asp-controller="Company" class="btn btn-primary">Yeni Company Ekle</a>
    <table class="table table-striped">
        <thead>
            <tr>
                <th style="width: 10px">#</th>
                <th>Companies</th>

                <th style="width: 40px">Düzelt</th>
                <th style="width: 40px">Silme</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
            }
            @foreach (var item in Model)
            {
                <tr>
                    <td>@i.</td>
                    <td>@item.CompanyName</td>

                    <td><a class="btn btn-success" asp-action="Edit" asp-controller="Company" asp-route-Id="@item.CompanyId">Düzelt</a></td>
                    <td><a class="btn btn-danger" asp-action="Delete" asp-controller="Company" asp-route-Id="@item.CompanyId">Silme</a></td>
                </tr>

                i++;

            }





        </tbody>
    </table>
</div>
<!-- /.card-body -->
</div>

